#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../markdown-it/bin/markdown-it.mjs" "$@"
else
  exec node  "$basedir/../../../../markdown-it/bin/markdown-it.mjs" "$@"
fi
# cmd-shim-target=D:/xyh/my-docs/node_modules/.pnpm/@vuepress+markdown@2.0.0-rc.30/node_modules/markdown-it/bin/markdown-it.mjs
