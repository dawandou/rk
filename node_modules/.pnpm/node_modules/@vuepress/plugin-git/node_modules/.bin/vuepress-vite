#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vuepress/bin/vuepress-vite.js" "$@"
else
  exec node  "$basedir/../../../../vuepress/bin/vuepress-vite.js" "$@"
fi
# cmd-shim-target=D:/xyh/my-docs/node_modules/.pnpm/@vuepress+plugin-git@2.0.0-_0d18c7e8209ecdd4a3bf55c9736dedb0/node_modules/vuepress/bin/vuepress-vite.js
