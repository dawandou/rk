#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vuepress/bin/vuepress.js" "$@"
else
  exec node  "$basedir/../../../../vuepress/bin/vuepress.js" "$@"
fi
# cmd-shim-target=D:/xyh/my-docs/node_modules/.pnpm/@vuepress+plugin-prismjs@2._70e16b41f422646a82b43cd542bc4fcf/node_modules/vuepress/bin/vuepress.js
