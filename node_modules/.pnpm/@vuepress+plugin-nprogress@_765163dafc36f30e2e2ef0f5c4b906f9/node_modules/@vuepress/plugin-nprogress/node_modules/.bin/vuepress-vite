#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vuepress/bin/vuepress-vite.js" "$@"
else
  exec node  "$basedir/../../../../vuepress/bin/vuepress-vite.js" "$@"
fi
# cmd-shim-target=D:/xyh/my-docs/node_modules/.pnpm/@vuepress+plugin-nprogress@_765163dafc36f30e2e2ef0f5c4b906f9/node_modules/vuepress/bin/vuepress-vite.js
