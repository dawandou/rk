#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vuepress/bin/vuepress-webpack.js" "$@"
else
  exec node  "$basedir/../../../../vuepress/bin/vuepress-webpack.js" "$@"
fi
# cmd-shim-target=D:/xyh/my-docs/node_modules/.pnpm/@vuepress+plugin-seo@2.0.0-_8fe5d3d3c2fc5dc7fa4bb04d0f8d7f43/node_modules/vuepress/bin/vuepress-webpack.js
