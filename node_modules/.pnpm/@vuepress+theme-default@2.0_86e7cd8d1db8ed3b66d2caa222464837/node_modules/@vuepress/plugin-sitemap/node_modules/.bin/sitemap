#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../sitemap/dist/esm/cli.js" "$@"
else
  exec node  "$basedir/../../../../sitemap/dist/esm/cli.js" "$@"
fi
# cmd-shim-target=D:/xyh/my-docs/node_modules/.pnpm/@vuepress+plugin-sitemap@2._f51f54df5676793219799214188aeff2/node_modules/sitemap/dist/esm/cli.js
